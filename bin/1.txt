反射的应用场合：
	在编译时根本无法知道该对象或类可能属于哪些类，程序只能靠运行时信息来发现该对象和类的关系
比如在Zorm的框架的类Query的插入数据库的语句里面就用到了反射，因为根本不知道要插入的对象是谁？有可能是Emp对象，有可能是Dept对象，还有可能是Student对象
因而通过反射获取这些类的方法和属性，即Class clazz=obj.getClass();因为Object是所有类的父类，因为父类引用指向子类对象，根据传入的对象来加载该对象类的信息